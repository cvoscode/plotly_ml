# Classification

This module provides visualization tools for classification model evaluation.

It supports:

- Binary classification (via `y_true` + `y_score`)
- Multiclass (one-vs-rest) when you pass per-class probability columns (`proba_columns`)

::: classification
